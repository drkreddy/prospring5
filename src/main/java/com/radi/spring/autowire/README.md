@Autowired can be applied on any one of
a) field
b) setter method.

@Resource(name="id") can be used instead of @Autowired

@Inject (javax.inject.Inject) is equivalent in behavior to Spring’s @Autowired annotation.